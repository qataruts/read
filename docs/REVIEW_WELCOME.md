# بوّابةُ العين — «المرجع التعريفيّ»، المرحلة الأولى

> **المرحلتان تامّتان.** بوّابةُ الحزمة مرحلتان (قرار المدير، ١٢ أغسطس ٢٠٢٦):
> **الأولى** بنيةً وخطّاً وصفحةَ المنهج — **حُكم فيها ومُرّت**؛ و**الثانية** بأحكامك
> الخمسة: Cairo مُقَرٌّ والخاسران محذوفان · الرئيسةُ **أُعيد تأليفُها** وبُنيت
> «الأسس» و«الدليل العملي» بحارس تغطية · اللقطاتُ السبع مولَّدةٌ بشرطَيها · رقمُ
> بنك الصوت مؤجَّلٌ · و`--device` امتدّ إلى `welcome/`. **وهذه الوثيقةُ سجلُّهما.**

## ١. ما يُنظَر فيه

| # | الشاهد | أين |
|---|---|---|
| ١ | **لوحةُ خطّ العناوين** — أربعةُ أعمدة، المادّةُ واحدة | `shots/welcome-fonts.png` (وتُعاد بناؤها من `tools/welcome_fonts.html`) |
| ٢ | **صفحةُ المنهج على الشاشة** | `shots/welcome-curriculum.png` · وحيّةً: `app/welcome/curriculum.html` |
| ٣ | **عيّنةُ الطباعة** — ورقةٌ تُسلَّم لمدرسة | `shots/welcome-curriculum-print.pdf` (١٦ صفحة) |
| ٤ | **الحارس** | `node tools/test_welcome.mjs` — **١٣٧ تحقّقاً** (كانت ٥١) |

## ٢. البنية المقترَحة — أربعُ صفحاتٍ بقشرةٍ واحدة

موقعٌ صغير، شريطُ تنقّلٍ واحد ثابتٌ فوق كل صفحة، وكلُّ صفحةٍ تقوم بنفسها بلا
جافاسكربت. **وما لم يُبنَ بعدُ يظهر في الشريط مُعلَناً بحاله** («الأسس · قريباً»)
— يُرى في البنية ولا يَعِد بنقرة، فلا رابطَ مكسور في صفحةٍ تُعرَض على مدرسة.

| الصفحة | الملفّ | دورُها | حالُها |
|---|---|---|---|
| الرئيسة | `welcome/` | البوّابة في ثلاثين ثانية — ما هو، لمن، ما يميّزه، لقطات، دعوة | **قائمة** (أُلحق بها الشريطُ وخطُّ العناوين وحدهما) |
| **المنهج** | `welcome/curriculum.html` | المرجع العميق: الرحلة محطةً محطة | **مبنيّةٌ كاملة — وهي محلُّ الحكم** |
| الأسس | `welcome/method.html` | المذهبُ العلميّ والمسوّغات وحدودُ النطاق | المرحلة الثانية |
| الدليل العملي | `welcome/guide.html` | للمعلم والوليّ: الصفّ والبيت، اللوحة، `?dev=1`، الخصوصية، التثبيت | المرحلة الثانية |

**والرئيسةُ لم تُفرَّغ بعد**: أقسامُها القائمة (الأسس الخمسة · دليل المعلم · التثبيت)
تبقى كما هي حتى تُبنى الصفحتان اللتان ترثانها — فلا تُحذف مادّةٌ قبل أن يوجد
مكانُها، ولا يبقى في الشجرة نصٌّ يتيم.

## ٣. خطّ العناوين — الحكمُ بالنظر

**الشرط**: عربيُّ الأصل (لا خطَّ لاتينيٍّ أُلحقت به العربية)، وترخيص OFL، ومضمَّنٌ
محلياً في `welcome/fonts/` (قانونُ صفر مرجعٍ شبكيّ قائم)، ويصلح **عنواناً في مرجع**
لا علامةً — فالعلامةُ لها خطُّها (Marhey) ولا يُنازَع.

| # | الخط | المصمّم | الطابع | مقطعا العربية واللاتينية |
|---|---|---|---|---|
| — | Baloo Bhaijaan 2 *(الحاصل اليوم)* | Ek Type | واجهةٌ مدوّرة، **لاتينيُّ الأصل** | (خطُّ التطبيق) |
| ١ | **Reem Kufi** | Khaled Hosny · Santiago Orozco | كوفيٌّ هندسيّ | ١٢ + ٢٠ ك.ب |
| ٢ | **Cairo** | Mohamed Gaber | سانس عربيٌّ حديث | ٣٠ + ٣٣ ك.ب |
| ٣ | **El Messiri** | Mohamed Gaber | إنسانيٌّ بأثر القلم | ٢٢ + ٢٤ ك.ب |

واللوحةُ **لا تعرض سطرَ عيّنة**: تعرض شرائحَ الصفحة نفسِها في كل خط (عنوانُ القسم
بشريطه · عنوانُ بطاقة محطة · رقمُ بطاقة إحصاء · ترويسةُ جدول)، وتحت كلٍّ **عيّنةُ
قراءةٍ بخطّ التطبيق** تُثبت أن مادّة الطفل لا تتبدّل بتبدّل العنوان.

### المنفَّذ: **Cairo** — وعلّتُه ثلاثةٌ رُئيت في اللقطة لا قيلت ذوقاً

1. **Reem Kufi يضيق بالمسافات**: في العنوان الطويل تلتصق الكلمةُ بجارتها
   («لماذا نبدأ بأربعة حروف» تُقرأ ككلمةٍ واحدة)، وحركاتُه تزدحم فوق الحرف. وهو
   خطُّ علامةٍ ولافتة، لا خطُّ عناوينِ مرجعٍ فيه اثنان وأربعون عنواناً.
2. **El Messiri تلتبس أرقامُه**: `٨` فيه قريبةُ الشبه بـ`٥` في المقاس المتوسط —
   وهذه صفحةٌ عمودُها الفقريّ **بطاقاتُ إحصاء**، فلا يُحتمل رقمٌ يُقرأ رقمين.
3. **Cairo يفي بالثلاثة**: يفصل الكلمات، ويحمل التشكيل نظيفاً، وأرقامُه المشرقية
   لا تلتبس. وهو متمايزٌ عن متن الصفحة (IBM Plex) بالثخانة والعرض فيبقى للعنوان
   صوتُه.

**والقرارُ لك**: الخطّان الآخران في `tools/fonts/` (عُدّةٌ لا تُنشَر)، فتبديلُ
الخيار سطرٌ واحد في `welcome.css` ونقلُ ملفَّين — وإن ثبت المختارُ **حُذف الخاسران**
كما فُعل في لوحة العلامة، فلا يبقى في الشجرة وزنٌ ميت.

**وثلاثةٌ لم تُمَسّ**: علامةُ «اِقْرَأْ» تبقى Marhey وحدَها، و**الأزرارُ تبقى بخطّ
التطبيق** (صوتُه لا صوتُ الورقة)، و**التطبيق لا يُمَسّ خطاً** — ملفّا Cairo في
`welcome/fonts/` خارج قشرة عامل الخدمة، فلا يحملهما جهازُ طفل.

## ٤. صفحةُ المنهج — كيف نُفِّذت قواعدُ العرض العشر

| القاعدة | التنفيذ |
|---|---|
| بطاقةٌ لكل محطة بنمطٍ **واحدٍ لا يتبدّل** | ١٥ بطاقة، ولكلٍّ *ماذا يتعلّم الطفل · كيف يعمل التمرين · دورُك أنت* بهذا الترتيب — **يفرضه الحارس** |
| خطٌّ زمنيٌّ بصريّ للرحلة | خمسُ مراحلَ كبرى في الرأس، وعددُ عقد كلٍّ **محسوبٌ من `journey()`** |
| لقطةُ شاشةٍ بجوار المحطة | ست لقطاتٍ من المولّد القائم (الخريطة · الدرس · الكلمات · القصة · السورة · اللوحة) |
| الأرقامُ في بطاقاتِ إحصاء | شريطان من ثمانٍ، و**٣٥ اسماً موسوماً `data-stat` في ٤٢ موضعاً يحسبها الحارس** |
| جدولٌ واحدٌ لكل قسمٍ أقصاه | سبعةُ جداول في سبعة أقسام — **لا قسمَ بجدولين** |
| لا فقرةَ فوق أربعة أسطر | متنُ الصفحة كلُّه فقراتٌ قصيرة، والتفصيلُ في البطاقات والجداول |
| عناوينُ تحمل المعنى | «لماذا نبدأ بأربعة حروفٍ لا بالألف والباء» · «باعدنا بين المتشابهات — ثم واجهناه بها قصداً» |
| مسارا قراءةٍ معلَنان | «في خمس دقائق» و«المرجع الكامل» ببطاقتَي قفزٍ في الرأس |
| طباعةٌ نظيفة | `@media print` كاملة — انظر §٦ |
| القوانين القائمة | صفرُ جافاسكربت · صفرُ مرجعٍ خارجيّ · لوحُ `app.css` (لا سداسيّ) · رموزُ `app/emoji/` — **الأربعةُ محروسة** |

## ٥. «لا محطةَ تُترك» — حارسٌ لا وعد

الجردُ المطلوب في التكليف طويل، و**الوعدُ بتغطيته لا يُصدَّق بلا حارس**. فلكل بطاقةٍ
وسمُ `data-covers` بنوع محطتها، ويثبت `test_welcome.mjs` أنّ **مجموعة الأنواع
المغطّاة هي عينُ مجموعة أنواع العقد في `journey()`** — لا نقصان ولا زيادة — وأنّ
العدد المكتوب في البطاقة هو عددُ عقد نوعها:

```
letter ٢٨ · words ٧ · skill ٦ · story ٣ · contrast ٢ · gate ٢ · quran ٣٠
prophet ١ · garden ١٠٠ · ladder ٨٠ · library ١٠ · roots ١٣ · shelf ٩   =  ٢٩١
```

**فنوعُ محطةٍ جديد يُضاف غداً يُسقِط الفحص يومَ يُضاف** — وهو نظيرُ `test_measure`
في بابه: يحرس الغياب لا الحضور.

وفوقه يحرس الفاحصُ **أنّ النصّ منقولٌ لا مُعادُ الصياغة**: قواعدُ الدروس الستة،
وسطورُ معاني الجذور الثلاثة عشر، وأسماءُ السور والبساتين وقصص المكتبة والرفّ،
وأسماءُ محطات المرحلة القرآنية **المحسوبةِ من البيانات** (التهيئة · رسمُ المصحف ·
سورٌ قصار ١/٢/٣) — تُقرأ من مصدرها وقتَ الفحص لا من ذاكرة كاتب.

**وعيّناتُ القراءة من بيانات المنهج نفسِها** (جملتان من السلّم وسطرٌ من أول قصة)
بخطّ التطبيق ومقاسه — «ما يراه الوليّ هو ما ستراه الطفلة». **ولا حرفَ من نصّ
المصحف في الصفحة**: نصُّه لا يُكتب بأيدينا، فما يُرى منه لقطةُ التطبيق — ويفرضه
الحارسُ بمقابلة الصفحة بآيات السور كلِّها.

## ٦. عيّنةُ الطباعة

`shots/welcome-curriculum-print.pdf` — يخرج من الصفحة نفسِها بلا ملفٍّ ثانٍ يُصان:
يسقط الشريطُ وروابطُ القفز والدعواتُ وترويسةُ الموقع، وتظهر **ترويسةُ مطبوعٍ**
(«منهج اِقْرَأْ — مرجعُ المعلم ووليّ الأمر · read.mishkat.qa»)، ولا تُقصّ بطاقةٌ ولا
صفُّ جدولٍ بين ورقتين، واللقطاتُ تبقى محدودةً بسبعة سنتيمترات فلا تبتلع الورقة.

## ٧. مرفوعاتٌ إليك — قرارات تنتظر حكمك

1. **الخطّ**: Cairo منفَّذ بعلّته أعلاه — يُقَرّ أو يُبدَّل بأحد المرشَّحَين.
2. **مصيرُ أقسام الرئيسة**: بعد بناء «الأسس» و«الدليل العملي» تُنقَل إليهما مادّةُ
   الرئيسة الحالية (الأسس الخمسة · دليل المعلم بجدوله ومسوّغاته الأربعة · باب الصفّ)
   وتبقى الرئيسةُ بوّابةً خفيفة. **أَأنقلها نقلاً أم أعيد تأليفها موسَّعةً؟**
3. **لقطاتُ المحطات الناقصة**: البطاقاتُ الآن ستُّ لقطاتٍ من ثلاث عشرة محطة —
   والتكليفُ يطلب لقطةً بجوار كل محطة. توسعةُ `make_welcome_shots.py` (الشجرة ·
   البوابة · ميّز بين · البستان · السلّم · الرفّ · الخفوت) **في المرحلة الثانية**
   كما نصّ قرارُك؛ أستأذن في عددها قبل التوليد.
4. **رقمُ بنك الصوت لم يُكتب عمداً**: جلسةُ الصوتيات تعمل الآن في `app/audio/`،
   وأيُّ رقمٍ يحسبه الحارسُ من `versions.json` يحمرّ مع كل دفعة. فوُصف النظامُ
   («بنكٌ محقَّقٌ بأذنٍ بشرية ودفترُ نسب») بلا عدد — ويُضاف بعد سكون تلك الجلسة.
5. **`--device` لا يغطّي `welcome/`**: صفحاتُ المرجع خارج عدّة الجهاز (شاشاتُ
   التطبيق وحدها فيها). قياسُ الفائض على الآيباد لهذه الصفحات مهمةٌ صغيرة مستقلّة
   إن أردتَها — والصفحةُ اليوم `overflow-x` محصورٌ في الجداول وحدها بالبناء.

## ٨. الخضرة

- `node tools/test_welcome.mjs` — **١٣٧/١٣٧** (كانت ٥١).
- **٢٤/٢٤ حارساً** من حرّاس node، و`check_decodable` و`check_lexicon`
  و`fetch_twemoji --check` (٥٧٣ رمزاً = ٥٧٣ ملفاً) و`make_welcome_shots --check`
  و`make_icons --check`.
- `python3 tools/browser_test.py --welcome` — **٣١/٣١**. (ورمشةٌ تُسجَّل: أوّلُ
  تشغيلٍ ردّ «انتظار طويل: تفعيل عامل الخدمة» ثم مرّ نظيفاً، ومرّ على `HEAD` بشجرة
  عملٍ نظيفة ٣١/٣١ — فليست من هذه الجلسة.)
- **ولم يُمَسّ**: `app/audio/` ولا `tools/audio_*` ولا `tools/browser_test.py`
  (نطاقُ الجلستين المتوازيتين).


---

# المرحلة الثانية — المنفَّذ بأحكامك الخمسة

## ١) الخطّ: Cairo مُقَرّ، والخاسران محذوفان

`tools/fonts/` **حُذف كلُّه** — لا وزنَ ميتاً في الشجرة، كما فُعل في لوحة العلامة.
واللوحةُ تبقى **مولَّدةً متى احتيج إليها**: `tools/welcome_fonts.html` باقٍ، ويقرأ
المختارَ **من موضعه المنشور** (`app/welcome/fonts/`) لا من نسخةٍ ثانية تفترق عنه،
وفي ترويسته **أمرا `curl`** يُعيدان الخاسرَين من مصدرهما بسطرين إن أُعيد فتحُ القرار.

## ٢) الرئيسة: أُعيد تأليفُها ولم تُنقَل — وحارسُ التغطية يثبته

**الصفحاتُ الأربع قائمةٌ الآن**، وشريطُ التنقّل يقود إليها كلِّها (لا «قريباً» بعد اليوم):

| الصفحة | خطابُها | ما فيها |
|---|---|---|
| `‎/welcome/` | زائرٌ عابر في ثلاثين ثانية | أربعةُ فروقٍ تُرى في أول جلسة · لمن هو · ثمانيةُ أرقام · أربعُ لقطات · **ثلاثةُ أبواب** |
| `curriculum.html` | معلّمٌ يريد التفصيل | الرحلة محطةً محطة (المرحلة الأولى، كما أُقرّت) |
| `method.html` | **لجنةٌ تُدقّق قبل أن تُقرّ** | ثمانيةُ أسسٍ بسندها · المسوّغاتُ الأربعة · حدودُ النطاق · المراجعُ وأثرُ كلٍّ |
| `guide.html` | **معلّمٌ يُشغّل في صفّ** | جلسةُ البيت · خمسُ قواعدَ للصفّ · اللوحةُ وكيف تُقرأ · القياسُ وما يعنيه · **جرّبه من أوله** · الخصوصية · التثبيت والنسخة |

**وإعادةُ التأليف ظاهرةٌ لا مدّعاة**: «الأسس الخمسة» التي كانت بطاقاتٍ في الرئيسة
صارت في «الأسس» **ثمانيةَ أقسامٍ بسندٍ وأثرٍ معلَن** (ومنها ما بقي في الرئيسة بصياغة
البوّابة: المفكوكية والصوت والخصوصية)، و«دليل المعلم» الذي كان جدولاً ومسوّغاتٍ
انقسم: **المسوّغاتُ وحدودُ النطاق** إلى «الأسس»، و**الصفُّ واللوحةُ والتثبيت** إلى
«الدليل» — ومعها ما لم يكن موجوداً أصلاً: **«كيف تجرّب التطبيق من أوله»**.

**وحارسُ التغطية** (`test_welcome.mjs §٧`): جردٌ من **٢٦ حقيقة** كانت معروضةً في
الرئيسة، ولكلٍّ **موضعُها الجديد** مكتوباً — يفشل الفحصُ إن سقطت واحدة. والمحروسُ
**وجودُ الحقيقة لا حرفيّةُ صياغتها**، فإعادةُ التأليف مسموحة والإسقاطُ ممنوع.

## ٣) اللقطاتُ السبع — بشرطَيها

`make_welcome_shots.py` صار **١٣ لقطة** (الخريطة · الدرس · الكلمات · القصة · السورة ·
اللوحة + **البوابة · ميّز بين · البستان · السلّم · الشجرة · الرفّ · الخفوت**)، ولكل
بطاقةِ محطةٍ في صفحة المنهج لقطتُها بجوارها.

- **الشرطان منفَّذان**: `loading="lazy"` وأبعادٌ مصرَّحة على **كل صورةٍ في الصفحات
  الأربع** (٨٠ صورة) — يفرضه الحارسُ في المصدر، و`--welcome` يقرؤه **على الشاشة**
  قبل أن يستعجل الصور (وإلا قرأ ما كتبه هو لا ما كتبته الصفحة).
- **والطفلُ المزروع بقي واحداً** — ولكن **تقدّم موضعُه**: اللقطاتُ السبع كلُّها
  محطاتٌ بعد موضعه القديم (القصة الأولى)، فطفلٌ واقفٌ هناك لا يصدُق عنده شيءٌ منها.
  فصار على **باب الرفّ**، وله **قراءاتٌ متباعدة** على كلمات آخر درجةٍ في السلّم
  بلغت بها كلماتُه ز٢ وز٣ — وهي عينُ ما يبني عدّادَ الخفوت في الرحلة، فلا تُختلَق
  حالٌ لا تُبلَغ. ولذلك تبدّلت `map.png` و`parent.png` مع الجديدة.

## ٤) رقمُ بنك الصوت: مؤجَّلٌ كما أمرتَ

لم يُكتب رقمٌ، ووُصف النظامُ («بنكٌ محقَّقٌ بأذنٍ بشرية ودفترُ نسب»). ويُضاف بعد سكون
جلسة الصوتيات رقماً **يحسبه الحارس** من `versions.json` لا تقريباً.

## ٥) `--device` امتدّ إلى `welcome/`

الصفحاتُ الأربع تُقاس الآن في **المقاسات الخمسة** مع شاشات التطبيق، في إطارٍ بمقاس
المنظور — **والمقيسُ الأفقيُّ وحدَه**: هذه مستنداتٌ تُسحَب رأسياً بطبعها، فطولُها ليس
عيباً والعيبُ أن تفيض عرضاً. **والنتيجة صفرُ فائضٍ أفقيّ وصفرُ إيموجي نظامٍ** في
الأربع، في المقاسات الخمسة.

## ٦) أمرا المالك في أثناء البناء — منفَّذان

1. **وجهُ «الفيل والكعبة» صار الكعبة** 🕋 لا الفيل: بطلةُ الخبر الكعبةُ المحفوظة لا
   الفيلُ الممنوع — وهو ما تبدأ به القصةُ وتنتهي إليه نصّاً. و**بُدِّل في المولّد لا
   في الملف** (`make_stories.py` ثم `--write`)، ومنه يُشتقّ الغلافُ فلا يفترقان —
   فسرى على الخريطة والغلاف وصفحة المنهج والمطبوع بتبديلِ حرفٍ واحد في المصدر.
2. **ترويسةُ المطبوع صارت علامةً ورابطاً**: أيقونةُ التطبيق + «اِقْرَأْ» + عنوانُ
   الصفحة + `read.mishkat.qa` **رابطاً يُنقر في الـPDF**. وهو **الخارجيُّ الوحيد** في
   الموقع كلِّه، ومُقيَّدٌ في الحارس بعينه: `src` و`<link>` لا خارجيَّ فيها البتّة،
   و`<a>` واحدٌ لا غير — لأن الأول يُجلَب عند الفتح والثاني يُفتَح إن نُقر.
3. **وللتطبيق بابٌ إلى المرجع** (أمرُ المالك الثالث): سطرٌ هادئ في **ذيل الخريطة**
   لا في صدرها — «تعريفٌ بالمنهج — للمعلّم ووليّ الأمر». **وهو انتقالُ صفحةٍ لا
   تنقّلُ تطبيق**، فلا يعمل دون إنترنت وهو مقصود: صفحاتُ عرضٍ للراشد لا تُثقل جهاز
   طفلٍ بميغاباياتها.

## ٧) الخضرة الختامية

- `node tools/test_welcome.mjs` — **١٨٢/١٨٢** (كانت ٥١ قبل الحزمة، و١٣٧ بعد الأولى).
- **٢٤/٢٤ حارساً** من حرّاس node · `check_lexicon` و`check_decodable` و`icon_audit`
  و`make_stories --check` و`fetch_twemoji --check` (٥٧٣=٥٧٣) و`make_welcome_shots --check`
  (١٣ لقطة) و`make_icons --check`.
- `browser_test.py --welcome` — **٤٥/٤٥** (كانت ٣١): الصفحاتُ الأربع تُفتَح وتُقرأ،
  وبطاقاتُ المحطات تُعدّ على الشاشة، وصفرُ طلبٍ خارجيّ، ولا يبتلعها عاملُ الخدمة.
- `--device` أخضرُ في المقاسات الخمسة (ومعه صفحاتُ المرجع) · `--map` ٢١/٢١ ·
  `--stories` و`--quran` خضراوان بعد تبديل الوجه.
- **ولم يُمَسّ**: `app/audio/` ولا `tools/audio_*` — وصفرُ إضافةٍ صوتية في المرحلتين.

## ٨) الموقوفُ عند بابك

1. **رقمُ بنك الصوت** — يُضاف بعد سكون جلسة الصوتيات (بندُك الرابع).
2. **الشواهد**: `shots/welcome-home.png` · `welcome-curriculum.png` ·
   `welcome-method.png` · `welcome-guide.png` · و**المطبوع** `welcome-curriculum-print.pdf`
   (١٦ صفحة بترويستها الجديدة).


---

# المهمّتان الختاميّتان (١٢ أغسطس ٢٠٢٦)

## ١) 🔴 الـ٤٠٤ — العلّةُ ثبتت، والإصلاحُ مُثبَتٌ على الحيّ

**التشخيص**: لا سيرَ نشرٍ، ولا استثناءَ `WELCOME`، ولا اسماً لا يطابق رابطاً —
**بل الملفّان لم يبلغا `origin/main` بعد**. والدليلُ من السجلّ لا من الرأي:

| الحدث | وقتُه |
|---|---|
| `46b98b0` — `curriculum.html` تدخل التاريخ | ٠٠:٤٩ |
| **نشرةٌ حيّة** على رأس `7d6daea` | ٠٢:٤٦ ← فالرئيسةُ والمنهجُ منشوران |
| `c7ae9cf` — `method.html` و`guide.html` تدخلان التاريخ | ٠٣:٠٦ |
| **نشرةٌ حيّة** على رأس `d88ef04` | ٠٣:٠٩ ← فالأربعُ منشورات |

**وفحصُك وقع في النافذة بينهما** (٠٢:٤٦–٠٣:٠٩): صفحتان منشورتان وصفحتان لا. و`pages.yml`
ينشر **عند الدفع** (`on: push`) وينسخ `app/` كلَّه — فلا قائمةَ ملفّاتٍ تُنسى، و**GitHub
Pages يردّ ٤٠٤ حقيقياً** لملفٍّ غير موجود (لا ردَّ تنقّلٍ يبتلعه)، فظهر العيبُ كما رأيتَه
بالضبط. **ولم تُنشَر `46b98b0` ولا `c7ae9cf` باسميهما**: الدفعةُ الواحدة تحمل التزاماتٍ
عدّة وتُنشَر نشرةً واحدة على رأسها — وهو سببُ غياب اسميهما من سجلّ الأثر.

**والصنفُ لا يمسكه فحصٌ على القرص أبداً**: «مُلتزَمٌ» ليست «منشورة». فبُنيت
**`tools/check_live.py`** — تجلب فعلاً وتقابل العنوانَ بعنوانه:

```
✓ /welcome/                   200 — «اِقْرَأْ — تعليم القراءة العربية لطفل السادسة»
✓ /welcome/curriculum.html    200 — «منهج اِقْرَأْ — الرحلة محطةً محطة»
✓ /welcome/method.html        200 — «أسسُ اِقْرَأْ — لماذا هذا الترتيب لا غيره»
✓ /welcome/guide.html         200 — «الدليل العملي — اِقْرَأْ في البيت وفي الصفّ»
✓ الأصول السبعة (التنسيق · لوح التطبيق · البيان · العامل · لقطة · خطّ · جذر التطبيق) 200
```

**وقائمتُها مشتقّةٌ من الشجرة** (`app/welcome/*.html`) لا مكتوبةً بيد — فصفحةٌ خامسة
تُكتب غداً تُفحَص حيّةً **يوم تُكتب**. و`--self-test` فيها **بلا شبكة** (تفحص العدّة:
القائمةَ مشتقّةً، والعنوانَ موجوداً، والأصولَ في الشجرة، وسيرَ النشر كما وُصف) فدخلت
**جردَ `test_selftests`** كأخواتها — «فحصٌ لا يُشغَّل ليس حارساً».

## ٢) رقمُ بنك الصوت — دخل محسوباً

**٢٩٧٦** ملفاً في الرئيسة وصفحة المنهج، **يحسبه الحارس**:
`Object.keys(versions.json).length` (٢٩٠٨ مولَّدة) `+ Object.keys(recitations.ayat).length`
(٦٨ تلاوة) — لا رقمَ بيد، ومطابقٌ لتحقّقك. ومعه **٦٨ آيةً** بوسم `ayat` المحسوب من
`QURAN.surahs` نفسِها، فيلتقي عدّادان من مصدرين مختلفين على الرقم نفسِه.

**وحُمرتُه المستقبلية مقصودة**: دفعةُ صوتٍ جديدة تُسقِط الفحصَ يومَ تُصرَّف — وهو
الحارسُ يعمل لا عيبٌ فيه، كما نصصتَ.

## ٣) الخضرة الختامية

`test_welcome` **١٨٥/١٨٥** · **٢٤/٢٤ حارساً** (ومنها `test_selftests` بستّ فحوصٍ
ذاتية) · الفاحصان و`icon_audit` و`make_stories --check` و`fetch_twemoji --check`
و`make_welcome_shots --check` (١٣) · `browser --welcome` **٤٥/٤٥** ·
**`check_live.py` على `read.mishkat.qa`: أربعُ صفحاتٍ وسبعةُ أصولٍ بـ٢٠٠**.
