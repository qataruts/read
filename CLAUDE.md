# مشروع «المُعلِّم» — تعليم القراءة العربية لطفل السادسة

تطبيق ويب (دون أدوات بناء) يعلّم الطفل القراءة على أساس القاعدة النورانية مطوَّعةً:
ترتيب الحروف بالتواتر، كلمات من عالم الطفل، مفكوكية ١٠٠٪، بمسار واحد ينتهي بالتهيئة القرآنية.

**الهوية (٨ أغسطس ٢٠٢٦)**: اسمُ المنتج **«اقرأ»** ورسمُ علامته مشكولاً **«اِقْرَأْ»**،
والدومين `read.mishkat.qa`، والوصف «اِقْرَأْ — يعلّم طفلك قراءة العربية، من الحرف الأول
إلى القرآن». وخطُّ العلامة Marhey لها وحدَها، ونصُّها الواحد `BRAND` ومُصيِّرها
`brandMark()` في `ui.js`. و**«المُعلِّم» يبقى اسمَ المشروع في السجلات** (هذا الملف
و`docs/` والالتزامات) — لا يُعاد كتابةُ الماضي. التفصيل في `docs/REVIEW_BRAND.md`
و`docs/DESIGN.md §٦`، وحارسُه `tools/test_brand.mjs`.

## طريقة العمل — اقرأ هذا أولاً
- **مدير المشروع**: جلسة فابل (Fable) — تضع المنهج والخطة وتراجع المنتجات. **جلسات التنفيذ**: أوبوس ٥.
- إن كنت جلسة تنفيذ: اقرأ `docs/METHOD.md` (المرجع الملزم للمنهج) ثم بند جلستك في `docs/SESSIONS.md`، ونفّذ حدود جلستك فقط، وحدّث حالة الجلسة عند الإتمام.

## خريطة الملفات
- `docs/METHOD.md` — المنهج التعليمي الملزم (المجموعات، حلقة الدرس، التدرج، استراتيجية الصوت)
- `app/js/install.js` — **«باب التثبيت»** (أمر المالك، ١١ أغسطس ٢٠٢٦: «اقرأ من المتصفح ليس عملياً — من التطبيق هو ما نريده»): شريطٌ أعلى التطبيق يقول لكل جهازٍ ما يخصّه — `beforeinstallprompt` يُمسَك فيُعرَض زرُّ «ثبّت الآن» **يفتح نافذة التثبيت الحقيقية** (أندرويد/كروم/إيدج)، وعلى iOS خطوتا سفاري نصّاً (وآيباد iPadOS يتنكّر «Macintosh» يفضحه `maxTouchPoints`)، ومن متصفّحٍ آخر على iOS: «افتح في سفاري أولاً»، وحيث لا طريقَ الصمتُ أهدأ. **ولا يظهر في التطبيق المثبَّت أبداً** (`standalone`/`appinstalled`)، والإغلاقُ يُرقده أسبوعاً. قرارُ العرض دالّةٌ نقيّة (`installState`) والوحدةُ لا تعرف الشبكة، ومعفاةٌ في المعاينة، وفي قشرة v21. وذكرُ الشريط في التعريف نصٌّ ساكن (الرئيسة والدليل) — لا سطرَ جافاسكربت فيهما. حارسه `tools/test_install.mjs`.
- **حرّاسُ التعريف يقارنون المعنى لا الهيئة** (١١ أغسطس ٢٠٢٦): مرّت صفحاتُ `welcome/` بمنسِّقٍ آليّ في محرّر المالك (التزام «update email») فتوزّعت السماتُ والنصوصُ أسطراً واحمرَّ `test_welcome.mjs` والمحتوى سليم. فصارت مطابقاتُه النصية على فراغٍ موحَّد (`flat()`) وسماتُه بـ`\s+` — **فالمحروسُ وجودُ الحقيقة لا حرفيّةُ صياغتها**، والمنسِّقُ يملك الهيئة.
- **«لا انتقالَ وكلامٌ في الجوّ»** (بلاغٌ عائليّ من «احسب»، ١١ أغسطس ٢٠٢٦ — قشرة v23): حرّاسُ متصفحنا كانوا **يعدّون التشغيلات ولا يقيسون التعاقب** — صوتُ عُدَدهم المزيّف ينتهي في ٥ مللي فلا تراكبَ يمكن أن يُرى أصلاً. فبُنيت عدّةُ `--voice` (`tools/browser_voice.html`): أصواتٌ بمدةٍ حقيقية (١١٠٠ مللي) ونقرُ «طفلٍ سريع» يجيب والسؤالُ في الجو، تقيس التراكبَ (لا لحظةَ فيها صوتان) و**القطشَ المبرمَج** (قصٌّ بلا نقرةٍ قريبةٍ تبرّره — وقطعُ الطفل بيده مشروع). **فقاست قطشين مسمَّيين**: سياجُ `setTimeout(750)` الثابت في محرّك المراجعة/البوابتين يقطش سؤالاً في الجو (٩٠٤ من ١١٠٠ مللي)، ونظيرُه في «ميّز بين» يقطش تتابعَ «اسمع الفرق» (ثانيتين). والعلاجُ قاعدةً في القناة: `tail`/`quiet()` و`afterSpeech(fenceMs, fn)` — **سكوتُ القناة ومهلةُ العين معاً**، فالتمرين الصامت ينتقل بمهلته كما كان حرفاً، والناطقُ يُتمّ كلامه؛ والنداءُ المبرمَجُ كالانتقال (نداءاتُ الجولات بعد سكوت ما قبلها)، وإعاداتُ «اسمع بعد الخطأ» بعد تمام صدى المختار لا فوقه. طُبّقت في سبع وحدات، و`words.js` و`story.js` و`ladder.js` **كانت على الصواب منذ بُنيت** (انتظارُ `ended` ثم مهلة — نمطُ احسب المعتمد نفسُه) فلم تُمسّ. **وجُرد «سباقُ الإسكات»** (علّةُ احسب الثانية: نداءان في لحظةٍ كلٌّ يُسكت ما لم يبدأ): لا موضعَ له عندنا — نداءاتُنا مفردة، ودلالةُ المتزامنَين في قناتنا «الأخيرُ يعيش» يثبتها المشهد ١ من `test_audio_stack`. الحارسان: `--voice` (**مُجرَّبٌ سالباً**: إسقاطُ انتظارٍ واحد يُحمّره بقطشٍ مسمّىً بالأرقام) والمشهد ٥ من `test_audio_stack.mjs`. **والقاعدة المقيَّدة: حارسٌ يعدّ التشغيلات ولا يقيس التعاقب أخضرُ كاذب.**
- **بلاغا الابنة — التكدّس والزوم** (١١ أغسطس ٢٠٢٦، قشرة v22): النقرُ المتكرر السريع كان يكدّس الأصوات بعدد النقرات، والعلّةُ في `audio.js` مركّبة: الإيقافُ المتعمَّد كان يسقط في `catch` إعادةِ المحاولة (المكتوبةِ لملفٍّ تأخّر) **فيُبعَث الموقوفُ فوق الجديد**، و`playSequence` كان يمضي إلى آخره ولو تُجووز. فصار الإيقافُ المتعمَّد **تسويةً هادئة** (`_hush` يحلّ الوعد بـfalse — وكان المتجاوَزُ أثناء السماع يبقى وعدُه معلَّقاً أبداً) وللتشغيل **جيلٌ** (`epoch`) يُبطل بقايا المتجاوَز، وبقيت الإعادةُ لعطبها الحقيقيّ. حارسه السلوكيّ `tools/test_audio_stack.mjs` (عنصرُ صوتٍ مزيّف، **مجرَّبٌ سالباً**: القديم يحمرّ ويتعلّق). و**الزوم**: iPadOS يسترجع المثبَّت من الخلفية مكبَّراً أحياناً (عيبُ منصّة) — عولج أولاً بقفل الميتا الشامل، **فنقضه بلاغُ ميدانٍ ثانٍ من يومه** (قشرة v24): الطفل كان يكبّر الصفحة بالقرصة ليرى التشكيل جيداً («أمرٌ مريح وتلقائي») والقفلُ حرمه إياها — تحويطٌ أوسعُ من عيبه، وافتراضُ «حروفُه كبيرة فلا حاجة للتكبير» أسقطه الميدان. فصار العلاجُ في موضع العيب وحدَه: **لا قفلَ في الميتا** (القرصة حق)، و`resetZoom` في `main.js` عند `pageshow`/`visibilitychange` يشدّ المقياس إلى ١ لحظةً (`maximum-scale=1`) ثم يردّ الحرية بعد ٨٠ مللي — فتزول بقايا الاسترجاع المعيب وبقايا تكبيرِ ما قبل الخلفية معاً، و`touch-action: manipulation` يبقى (يُسقط تكبيرَ النقر المزدوج العارض وحدَه ومهلةَ انتظاره، والقرصةُ لا تُمَسّ). حارسُه القسم ٥ من `test_pwa.mjs` — **وكان يحرس القفلَ فقُلب: بلاغُ الميدان فوق الحارس**.
- **فحصٌ بقدر العمل أثناءه، وكاملٌ عند بابه** (أمر المالك عبر بريد العائلة، ١٣ أغسطس ٢٠٢٦ — «الفحوصُ الكبرى عند الرفع والنشر فقط»): ثلاثُ طبقات — **حرّاسُ node/python في كل جلسة دائماً** (٢٨ حارساً بلا متصفح، ثوانٍ)؛ **وبطاريةُ المتصفح بقدر ما لُمس** (شاشةٌ أو تنسيقٌ أو سلوك: تُشغَّل نطاقاتُها المعنيّة وحدها — `--parent` لمن مسّ اللوحة، `--voice` لمن مسّ التعاقب…) وجلسةٌ لم تلمس شاشةً لا تشغّلها؛ **والكاملةُ عند الدفع إلى المنشور**. **والشرطُ الأعلى لا يُمَسّ: لا يُدفَع أحمر** — تقليمُ ما يُشغَّل أثناء العمل شيءٌ، وتخفيفُ بوابة النشر شيءٌ ممنوع.
- **همزةُ الوصل: القاعدةُ لِما يُنطَق ولا يُعرَض** (بلاغُ اكتب العائليّ وحكمُ مدير المجموعة، ١٣ أغسطس ٢٠٢٦): مولّدُ الصوت ينطق همزةَ الوصل المشكولة حركةً مستقلّةً مطوَّلة (قاسه اكتب: ثلثُ مدّة التعليمة)، فقاعدتُهم أن تُكتب عاريةً في النصّ المنطوق. **وعندنا تُقيَّد بحدّها**: تسري على **نصوص التعليمات المنطوقة وحدها**، ولا تمسّ **مادّةَ القراءة المعروضة على الطفل** — فكلُّ حرفٍ يراه مشكولٌ بحكم المفكوكية، وتعريةُ همزةٍ في كلمةٍ يقرؤها نقضٌ لأصلٍ أكبر. وجردُ بنكنا (٢٩٠٨ نصاً) وجد ستةً: ثلاثةٌ **بطاقاتُ الألف بحركاتها** (مادّةُ تهجٍّ لا همزةَ وصلٍ فيها)، وثلاثٌ **كلماتُ معجمٍ تُقرأ** (اِبْتِسَامَةْ · اِمْتِحَانْ · اِبْتِسَامَتِي) — **فلا موضعَ للقاعدة عندنا اليوم**، وتبقى حارساً على ما يُستجدّ من تعليمات.
- `docs/SESSIONS.md` — خطة الجلسات ومعايير قبولها وسجل حالتها
- `docs/AUDIO_QUEUE.md` — بروتوكول فصل الصوت عن التطوير (ملزم)
- `app/js/curriculum.js` — **مصدر الحقيقة الوحيد** لبيانات المنهج (حروف/مجموعات/كلمات/مقاطع/مهارات/قصص/المرحلة القرآنية)
- `app/data/lexicon.json` — معجم «حديقة الكلمات» (طبقة ب١): ٢٥٠ كلمة في عشرة بساتين، ومعها **جمل سلّم الجمل** (جملةُ مثالٍ لكل كلمة + `sentences`: ١٦٠ جملة متدرّجة من ٣–٥ كلمات) و`support` (معجم الجمل المساند المعلَن). يقرؤه `app/js/lexicon.js` ويفحصه `tools/check_lexicon.py`. **يوسّع رصيد المنهج ولا يكرّره** (يرفض الفاحص كلمةً درسها الطفل في `curriculum.js`)، و**مقاطعه مشتقّة** بمقطِّع نورانية لا مكتوبة بيد (`--fill-tiles`).
- **«رفُّ المكتبة»** (حزمة المكتبة، ١٢ أغسطس ٢٠٢٦ — أمر المالك «حجمُ القراءة»، وأحكامُ المدير الخمسة على `docs/REVIEW_LIBRARY.md`): الطلاقةُ لا تُبنى بالتمارين بل بأميال قراءةٍ حقيقية، وقصصُ البساتين العشر **١٨٥ كلمةً** كلُّها. فصار للقصة **محورُ موضعٍ ثالث** `shelf` — قسمٌ واحد في **ذيل الرحلة** بعد البساتين وسلالمها ومكتباتها وأشجارها، رصيدُه الرحلةُ كلُّها (٧٩١ مفردة معلَنة) فهناك وحدَه تحتمل القصةُ الطول؛ و`garden: play` **تكذب البيانات** وتفرض قيداً فنياً على المعنى. و**سلّمُ الطول ٤–٥–٦** (٩–١٠ صفحات/٣–٦ كلمات · ١١–١٢/٤–٧ · ١٣–١٤/٤–٨) يملكه الفاحص كالقائم: أكبرُ نموٍّ فيه ×١٫٥ دون قفزتَي القائم (×١٫٩). و**رافدان**: تأليفٌ مقيَّد، و**تراثٌ عامّ يُعاد تأليفُه بالعقد نفسِه** (لا حرفَ يُنقل من نصٍّ منشور — حقوقٌ ومفكوكية) — وأبطالُه لا يطلبون إعلاناً لأنّ `أَسَدْ` و`ثَعْلَبْ` و`غُرَابْ` و`جَرَادْ` و`نَمْلْ` كلماتُ **منهج**. و**سؤالٌ لكل مقطع**: `questions` قائمةٌ لكلٍّ `upto`، و`segments_of` تحسبها ⌈الصفحات ÷ ٥⌉ فلا رقمَ يُكتب بيد — و**المقطعُ وحدةُ العرض** فلا تفيض عشرُ صفحاتٍ عن شاشة الآيباد، وجوابُه ممّا قرأه **إلى صفحته** لا ممّا بعدها. و**حوضُ خياراته معجمٌ ومنهجٌ معاً** («ألّا تُسأل قصةُ الأسد عن الأسد» عيبٌ ظاهر)، وبه انكشف أنّ `شَجَرَةْ` (منهج) و`غَابَةْ` (معجم) **صورةٌ واحدة** 🌲 فصارت الغابةُ `pictured: false` — إذ لا صورةَ صادقة لغابةٍ في يونيكود. و**لا صوتَ لجملةٍ فيه** («القصةُ تُقرأ لا تُسمع»: يسقط الكاريوكي وأذنُ السطر، وتبقى نقرةُ الكلمة شبكةَ الأمان الوحيدة)، ويُختَم بخطوة **«اِقْرَأْ لِأُمِّكْ»** بكتلة `record.js` نفسِها — خطوةٌ في الشاشة لا عقدةٌ ثانية، كنظيرتها خطوةِ الترديد. ونوعُ محطةٍ جديد `shelf` بإعفاءٍ **مكتوبٍ بسببه** في `test_measure`. و**أغلفةُ القصص** (أمر المالك، ١٢ أغسطس ٢٠٢٦): غلافٌ واحد لكل قصةِ رفّ — **بيانٌ لا صورة** (`cover: {hero, mood}` في ملف القصة) يركّبه `coverEl` في `ui.js` من رموز `app/emoji/` أنفسِها بـ CSS، فلا ملفَّ يُرسم بيد والحجمُ كيلوبات. **والشرطُ أن يبدو غلافاً لا أيقونةً مكبَّرة**: نسبةُ كتاب (٣:٤)، وأرضيةٌ من لوح `app.css` يترجمها **مفتاحُ المزاج** (لا لونَ منسوخ في بيان)، و**بطلٌ واحدٌ كبيرٌ في الوسط** — بُسِّط بأمر المالك بعد معاينة اللقطة: كان معه مسانِدان فبانَ أنّهما **رموزٌ مستقلّة تُصفّ لا عناصرُ لوحةٍ تُركَّب**، ورمزٌ واحدٌ واضحٌ خيرٌ من ثلاثةٍ تتزاحم. و**العنوانُ نصٌّ حقيقيّ** في DOM بخطّ العناوين في **شريطٍ ثابت الارتفاع** (يحتمل سطرين فتستوي البطاقاتُ على الرفّ). و**«صدقُ الصورة» يسري**: بطلُه من رموز قصته نفسِها (يفرضه الفاحص) ويجرده `icon_audit` بدرجة ج. و**لكل قصةٍ غلافُها بلا استثناء** (أُلحقت العشرُ القديمة ثم قصةُ السورة بأمر المالك): والبطلُ **وجهُ القصة نفسُه** لا يُكتب مرّتين فيفترقا يوماً — فشرطُ «صدق الصورة» مستوفىً بالبناء — والمعلَنُ في البيان **المزاجُ وحدَه**، وهو ما لا يُشتقّ. **ولا محتوى جديد فيه**: كسوةٌ لِما هو مكتوب، ولذلك لم يحتج غلافُ قصة السورة إلى البوّابة الثلاثية التي عبرتها مادّتُها. ويُعرَض **أغلفةً على رفّ** في قائمة الرفّ ومكتبات البساتين (فيصير الاسمُ صورةً لا استعارة، والمقفلُ يهتزّ ويُجيب كسائر المقفل) وفي صدر شاشة القصة. **وصفرُ إضافةٍ صوتية**. حرّاساه `tools/test_stories.mjs` و`browser_test.py --stories`، و**بوّابةٌ لكل مستوى** (`review_stories.mjs --shelf --level=N` ← `docs/REVIEW_SHELF<N>.md`) — «ثلاثُ قصصٍ في كل بوابةِ مراجعة أصدقُ فحصاً من تسع». و**صدقُ الخبر كصدقِ الصورة** (تصحيح المالك، ١٢ أغسطس ٢٠٢٦): لا يُوضَع في فم الراوي ما ليس حقاً — الطفلُ يصدّقه بلا مناقشة. كانت «يَتْرُكُ الْعِنَبَ الْحَامِضْ» شهادةً كاذبة (العنبُ سليم، وإنما الثعلبُ يعتذر لنفسه) فصارت «الْبَعِيدْ»، **وتبدّل المشهدُ معها** (🙄 كانت سخريةَ الأصل في صورة). والعبرةُ صارت «لا تحزن على ما فاتك، اطلب غيره».
- `app/data/stories/` — مكتبة «مصنع القصص» (الحزمة ٩): فهرسٌ (`index.json`: معجمُ المكتبة المعلَن + ترتيب القصص) وملفٌّ لكل قصة (مستوى، محورُ موضع، عنوان، صفحات، أسئلةُ مقاطع). يقرؤها `app/js/library.js` ويفحصها `check_stories` في `tools/check_lexicon.py`، **وتُؤلَّف بـ`tools/make_stories.py` لا تُحرَّر بيد** (عقد ٩أ نفسه، واشتقاقُه مستوردٌ من `make_sentences.py`). حدودُ المستويات (`STORY_LEVELS`) يملكها الفاحص ويطيعها المولّد. وبوّابةُ الاعتماد إلزامية: `tools/review_stories.mjs` ← `docs/REVIEW_STORIES.md` — **ولا قائمة صوت قبل حكم المدير عيناً**. و**للقصة محورا موضعٍ لا محور** (حزمة قصص الأنبياء، ١١ أغسطس ٢٠٢٦): `garden` لقصص المكتبة بعد البساتين، و`surah` لقصص المرحلة القرآنية — يملأ أحدُهما ويفرغ الآخر. وقصةُ السورة **تسبق محطة كلماتها** («لا سورةَ قَصَصيّةٌ قبل قصتها»)، وتقع **قبل البساتين كلِّها** فميزانيتُها ٧٤ كلمةَ منهجٍ و١٩٧ مفردةً مساندة لا معجمَ بستان — إلا بـ**«التقديم المعلَن»**: كلمةٌ يسقط بغيابها المعنى، في قائمةٍ معدودةٍ يطبعها الحارس، وتبقى في بستانها. وخياراتُ سؤالها من كلمات المنهج (`curriculumWord`). وبوّابتُها **ثلاثية**: فاحص ← مدير ← **مالك وجوباً** (`docs/REVIEW_PROPHETS.md`) — محتوىً دينيّ لا يكفي فيه الفاحص. و**وجهُها الكعبةُ لا الفيل** (أمر المالك، ١٢ أغسطس ٢٠٢٦): بطلةُ الخبر الكعبةُ المحفوظة لا الفيلُ الممنوع — وهو ما تبدأ به القصةُ وتنتهي إليه نصّاً («اللَّٰهُ يَحْفَظُ الْكَعْبَةْ»)، فالوجهُ يصدُق المعنى لا الحادثة. **ويُبدَّل في المولّد لا في الملف** (`emoji` في `make_stories.py` ثم `--write`)، ومنه يُشتقّ الغلافُ فلا يفترقان، ويسري على الخريطة والغلاف والمرجع التعريفيّ معاً.
- `tools/make_sentences.py` — مولّد «الجمل المتدرجة» المقيَّد (الحزمة ٩أ، وهي بروفة عقد مصنع القصص): **لا يُكتب فيه حرفٌ مشكولٌ بيد** — الجملة قائمةُ كلماتِ أساسٍ معلَنة + أدوارٍ إعرابية، والصيغة تُشتقّ. خطّ الإنتاج: توليد ← `check_lexicon.py` يرفض ما خالف ← مراجعة المدير (`tools/review_sentences.mjs` ← `docs/REVIEW_9A.md`). و`--check` يثبت أن ما في المعجم هو عينُ خرج المولّد. **وهو مالكُ «لباس الموضع»** (`dress_of`/`dressed`/`dressless`): صيغةُ كلمة المعجم في جملتها، يستهلكها الفاحص، ونظيرُها في التطبيق `dressOf`/`dressed` في `sentences.js` — به يُعرَض خيارُ «أكمل الجملة» **بالرمز الذي سيملأ الفراغ** حرفاً بحرف (إصلاح عيب ٦ أغسطس ٢٠٢٦)، ويُلبَس المشتّتان ثوبَه نفسَه.
- `app/sw.js` + `app/manifest.webmanifest` + `app/icons/` — العمل دون إنترنت (PWA)؛ الأيقونات تُولَّد بـ`tools/make_icons.py` من `tools/icon.html`
- `app/emoji/` + `tools/fetch_twemoji.py` — **«أيقونات لا إيموجي»** (أمر المالك، ٧ أغسطس ٢٠٢٦): رموزُ البيانات تُرسم من **Twemoji** (SVG، CC-BY 4.0) مخزونةً هنا لا من خطّ الجهاز — فإيموجي النظام صورةٌ لكل جهاز لا صورةٌ واحدة، ينقلب بها حكمُ «صدق الصورة»، والرموزُ الحديثة (يونيكود ١٤–١٥) تظهر مربّعاً فارغاً في الأجهزة الأقدم. **يُجرد المستعمَل وحدَه** من مصادره (المنهج والمعجم والقصص والصفحة التعريفية) فيُنزَّل ويُفهرَس في `emoji/index.json` — ومنه يخزّنه `sw.js` (كالأصوات والقصص من فهارسها). **والبيانات لم تتغيّر بحرف**: الرمز كما هو، وإنما تبدّل مَن يرسمه. أما **رموزُ الواجهة** (السماع، الاحتفال، القفل، الإعادة…) فأيقوناتُنا الخطية في `ICONS` بـ`ui.js` لا Twemoji — لغةُ الواجهة SVG خطيّ أصلاً. **والمُصيِّر واحد**: `faceEl` في `ui.js`، ولا موضعَ يكتب رمزاً في DOM سواه. المبدأ في `docs/DESIGN.md` §٦، وحارساه `tools/test_emoji.mjs` (الشيفرة) و`browser_test.py --device` (الشاشة نفسُها: صفر إيموجي نصّاً في كل شاشات الرحلة).
- `app/welcome/` — **«المرجع التعريفيّ»** (للعرض على المعلمين والمدارس): **موقعٌ من أربع صفحاتٍ بقشرةٍ واحدة وشريطِ تنقّلٍ واحد** — `index.html` (البوّابة في ثلاثين ثانية) · **`curriculum.html`** (المنهج محطةً محطة) · **`method.html`** (الأسس والمسوّغات وحدودُ النطاق) · **`guide.html`** (الدليل العملي: البيت والصفّ، ولوحةُ الوالد، و`?dev=1`، والتثبيت والنسخة) — و`welcome.css` و`shots/` (١٣ لقطة). و**الرئيسةُ أُعيد تأليفُها ولم تُنقَل** (حكم المدير): الرئيسةُ تخاطب زائراً عابراً، والأسسُ لجنةً تُدقّق، والدليلُ معلّماً يُشغّل — **ويحرس ألّا تسقط حقيقةٌ في النقلة جردُ تغطيةٍ** في `test_welcome.mjs` (٢٦ حقيقةً ولكلٍّ موضعُها الجديد). و**نمطُ البطاقة واحدٌ لا يتبدّل** في المرجع كلِّه: *ماذا يتعلّم الطفل · كيف يعمل التمرين · دورُك أنت*. و**«لا محطةَ تُترك» حارسٌ لا وعد**: وسمُ `data-covers` على كل بطاقة، ويثبت الفاحصُ أنّ مجموعة الأنواع المغطّاة هي عينُ أنواع عقد `journey()` بأعدادها — فنوعٌ جديد يُسقِطه **يومَ يُضاف** (نظيرُ `test_measure` في حراسة الغياب). و**النصُّ منقولٌ لا مُعادُ الصياغة** (قواعدُ الدروس ومعاني الجذور وأسماءُ محطات القرآنية المحسوبة)، و**عيّناتُ القراءة من بيانات المنهج** بخطّ التطبيق، و**لا حرفَ من نصّ المصحف في صفحةِ عرض** (صورتُه لقطةُ تطبيق لا نسخٌ بيد). و**تُطبَع طباعةً نظيفة** بترويسةٍ فيها **أيقونةُ التطبيق وعلامتُه وعنوانُ الموقع** (أمر المالك) — وهو **الرابطُ الخارجيّ الوحيد** في الموقع: `<a>` يُفتَح إن نُقر ولا يُجلَب. و**خطُّ العناوين Cairo** (OFL، عربيُّ الأصل، في `welcome/fonts/`) بعد لوحةِ ثلاثة مرشَّحين مُصيَّرةً (`tools/welcome_fonts.html`؛ Reem Kufi يضيق بالمسافات، وEl Messiri تلتبس أرقامُه ٨/٥ — `docs/REVIEW_WELCOME.md`) — والأزرارُ بخطّ التطبيق، والعلامةُ Marhey، **والتطبيق لا يُمَسّ خطاً**. و**للتطبيق بابٌ إليه**: سطرٌ هادئ في **ذيل الخريطة** (`map-about` في `main.js`) — انتقالُ صفحةٍ لا تنقّلُ تطبيق، فلا يعمل دون إنترنت وهو مقصود. **خارج التطبيق وخارج قشرة عامل الخدمة عمداً**: لا في `SHELL`، ويستثنيها `sw.js` من الاعتراض كلِّه (`WELCOME`) فلا تُخزَّن **ولا يبتلعها ردُّ التنقّل** — ولولا ذلك لفُتح التطبيقُ مكانَها على كل جهازٍ ثبّته. ولا تسجّل عاملاً ولا تصل بيان التطبيق ولا تحمل سطر جافاسكربت واحداً، ولا مَوردَ شبكيّاً خارجياً يُجلَب: لوحُها من `app/css/app.css` نفسِه (لا لون منسوخ)، **ولقطاتُها من شاشات التطبيق نفسِها** يولّدها `tools/make_welcome_shots.py` (والطفلُ المزروع **واحدٌ في الثلاث عشرة**)، وأرقامُها يحسبها الحارس، **ورموزُها صورُ `app/emoji/`** لا محارفَ خطّ النظام، **ولكل صورةٍ أبعادُها و`loading="lazy"`**. حرّاسُه: `tools/test_welcome.mjs` و`browser_test.py --welcome`، و**`--device` يقيس فائضَه الأفقيّ** على مقاسات الآيباد الخمسة (المستندُ يُسحَب رأسياً بطبعه فلا يُقاس طولُه). و**`tools/check_live.py` يفحص المنشور لا القرص**: صنفُ «مُلتزَمٌ ليست منشورة» لا يمسكه فحصٌ محلّيّ أبداً — يوم ١٢ أغسطس ردّت صفحتان **٤٠٤** على الحيّ بينما أختاهما ٢٠٠، والعلّةُ أنّ `pages.yml` ينشر **عند الدفع** (`on: push`، وينسخ `app/` كلَّه) والملفّان لم يبلغا `origin/main` بعد. فالأداةُ تجلب فعلاً وتقابل عنوانَ كل صفحةٍ بعنوانه، و**قائمتُها مشتقّةٌ من `app/welcome/*.html`** فصفحةٌ جديدة تُفحَص حيّةً يوم تُكتب، و`--self-test` فيها **بلا شبكة** فتدخل جردَ `test_selftests`. **ورقمُ بنك الصوت محسوبٌ** من `versions.json` و`recitations.json` (٢٩٠٨ مولَّدة + ٦٨ تلاوة) — ودفعةٌ جديدة تُحمِرّ الفحصَ يومَ تُصرَّف، وذاك الحارسُ يعمل لا عيبٌ فيه.
- **لفظُ الجلالة ومحرفُ التطويل** (حكم المدير، ١١ أغسطس ٢٠٢٦): يُكتب `اللَّٰهْ` — الألفُ الخنجرية على اللام مباشرةً **بلا تطويل** (`U+0640`). والعلّةُ كشفها الخفوت لا العين: موادُّ التأليف تمرّ به وز٣ ينزع التشكيل، فـ`اللَّـٰهْ` تصير «اللـه» بتطويلٍ معلَّق، و`اللَّٰهْ` تصير **«الله»** صحيحة. فالقاعدة: **يُكتب ما يبقى صحيحاً عارياً**، لا ما يصحّ مشكولاً وحده — ويحرسها `check_tatweel` في `check_lexicon.py` (لا `U+0640` في معجمٍ ولا قصةٍ ولا جملة)، واستثناؤه المعلَن نصُّ المصحف وحقولُ `face`. و**لفظُ الجلالة لا يُنوَّن**: `~N`/`~A` عليه يوقفان المولّد بنيوياً.
- **«الصوتُ يسمّي ولا يقرأ الرمز»** (أذنُ المالك، ١١ أغسطس ٢٠٢٦): **لا يُقتبَس رمزٌ مفرد في نصٍّ يُنطَق**. فقاعدةُ درس الهمزة كانت تقتبس الرموز بين قوسين، وهي على الورق سليمة — وفي الأذن نطقها المولّد «ء» «ها»، **لأنّ الرمز المفرد لا اسمَ منطوقاً له فيخمّنه**؛ وطالت سبع عشرة ثانية. فشُقّت قاعدتين قصيرتين على بطاقتيهما (**عشرُ ثوانٍ وقاعدةٌ واحدة في النفَس** — قاعدة المدير)، تُسمّى فيهما العلامةُ بلسانها («على ألف») و**تبقى صورتُها معروضةً على الشاشة تُقرأ بالعين**. وحارسُه `check_spoken_symbols` في `check_lexicon.py` — أخو `check_tatweel` وصنفُهما واحد: **ما يصحّ على الورق ولا يصحّ في الأذن**؛ ويقرأ جردَ المنطوق وحدَه، فما يُعرَض ولا يُنطَق (صورُ الحروف وأمثلةُ الرسم ووجوهُ العقد) خارجه بناءً لا استثناءً.
- `tools/generate_audio.py` — توليد أصوات mp3 إلى `app/audio/` (استبدالها بتسجيلات بشرية لاحقاً لا يغيّر الشيفرة)
- `tools/quran_source.txt` + `tools/sync_quran.py` — نصّ المصحف المرجعي (مشروع تنزيل، الرسم العثماني) وأداة نسخه إلى المنهج حرفاً بحرف
- `tools/fetch_recitation.py` → `app/data/recitations.json` + `app/js/recitation.js` — تلاوة القارئ (الحصري المرتّل) لآيات المرحلة القرآنية: **مسار مستقلّ عن المولّد وعن `manifest.json`**، وحدَه يُسمع نصَّ المصحف (لا مولّد ولا نطق آلي — انظر `AUDIO_QUEUE.md`)
- **«الجسر القرآني»** (الحزمة ١٢ — «لا سورة قبل كلماتها، كما تفعل النورانية»؛ ووسّعته حزمةُ «القرآني الموسّع» إلى اثنتي عشرة سورة — انظر بندَها أدناه): كلمات المرحلة الإملائية صارت **٢٤ كلمة على ثلاث درجات** (`QURAN.words.levels` في `curriculum.js`، حدُّ كلٍّ عددُ حروفها ٣←٤←٥ فأكثر يفرضه الفاحص)، و**محطةُ «كلمات السورة» قبل كل سورة** (`renderSurahWords` في `quran.js`، عقدتها `quran:sw-<سورة>`): كلماتُها **مشقوقةٌ من نصّ آياتها** بـ`surahWords()` — لا تُكتب بيد أحد ويرفض `check_decodable.py` كلمةً ليست في نصّ سورتها بالمصدر المرجعي — تُقرأ بطاقاتٍ تُسمَع كلمةً-كلمة بصوت قارئ، ثم جولاتُ «جِدْها في الآية» (لا صوت قبل الاختيار، والخطأ يُسمعه ما نقره هو). **والسورةُ مقفلة حتى تُتمّ كلماتُها** (القفل التسلسلي، ويحرس موضعَها `test_quran.mjs`). صوتُ الكلمة **تلاوةُ قارئ حصراً** يجلبها `tools/fetch_word_recitation.py` — **مقفلةٌ ببوّابة ترخيص** (`--approve-license` + إسنادٌ في `CREDITS.md`) ولم تُجلب بعدُ، فالمحطة تعمل صامتةً. وملفُّ الكلمة موسومٌ `wbw-<مفتاح>.mp3` فلا يلتقي بملفٍّ مولَّد له المفتاح نفسُه («مَا» و«مِنَ»).
- `app/js/fade.js` + عدّاد `reads` في `progress.js` — **خفوت التشكيل ز١→ز٣** (حزمة «الخفوت»، METHOD §٥.٧): جسرُ الطفل إلى النصّ العاري، **ولكل كلمة عتبةُ خفوتها الخاصة بتاريخه معها** — ثلاثُ قراءاتٍ صحيحة **متباعدة** (يومٌ واحد لا يزيد العدّاد إلا مرة، و**قاعدةُ الشاهد الواحد**: لا تُحتسب قراءةُ كلمةٍ إلا من موضعٍ شهد لها وحدَها — «رتّب» في موضعها و«أكمل» بكلمة الفراغ و«اقرأ واختر»؛ وسقط «اقرأ ونفّذ» وسؤالُ الفهم، فنقرةٌ بين ثلاث صورٍ شاهدٌ على الجملة لا على كلماتها. و`credit` تقبل كلمةً واحدة لا قائمة، فالحصانةُ بنيوية) ⇒ ز٢ الهيكلُ بمفاتيحه (تبقى الشدّة والتنوين والمدود)، وضِعفُها ⇒ ز٣ عارٍ. و**الشكل عند الطلب**: نقرةٌ تكشف الشكل ثوانيَ ويُسجَّل الكشف **تراجعاً جزئياً** فترتدّ الكلمة درجةً. مفتاحُ الكلمة جذعُها (`stemOf` نفسُه) فتاريخُها في الجملة والبطاقة واحد. يسري على القصص والجمل وبطاقات البساتين، **ويُحرَّم على** موادّ التهجّي ونصّ المصحف **وحوضِ الخيارات كلِّه** (فرقُ التشكيل بين الخيارات يفضح الجواب). و**صفر إضافة صوتية**: الصوت صوتُ الكلمة بأصلها. الاتجاه أحاديّ: `fade.js` ← `progress.js`، وحصانةُ المحظور بنيويةٌ (الوحدة لا تستورده أصلاً) يفحصها `tools/test_fade.mjs` على المصدر و`browser_test.py --fade` على الشاشة نفسِها.
- `app/js/skill.js` + `mark-<الدرس>` في `curriculum.js` — **قياس العلامات** (METHOD §٦.١): كانت دروسُ المدّ والسكون والشدّة والتنوين واللام **لا تسجّل مهارةً واحدة**، فبوابتا الإتقان ولوحةُ الوالد عمياوان عن أصعب أجزاء فكّ الشيفرة. فصار لكل درسٍ **مفتاحُه** بسابقة `mark-` (لا حرفَ وهميّ — سابقةُ الجذور وعلّتُها)، و**مفتاحٌ لكل درسٍ لا لكل علامة**: المدُّ درسان متباعدان، ودمجُهما يُجيز سؤالاً عن مدٍّ لم يُدرَس. و**تمرينان لا واحد**: `mark-compare` **قراءةٌ صامتة** («أيُّهما مَمدود؟» — الخياران طرفا الزوج مكتوبين بلا صوت، وهو الشطرُ الثاني من خطوة «قارِن») و`mark-quiz` تمييزٌ بالأذن. يدخلان ليتنر والمراجعةَ اليومية (`markItem` في `review.js`) والبوابتين، ولهما **قسمُ «العلامات»** في لوحة الوالد بأدنى الصندوقين (`markStats` في `parent.js`). **وصفر إضافة صوتية**: الأزواج لها ملفاتها منذ الجلسة ١، ووسمُ السؤال من `compare.labels` نصُّ واجهةٍ لا يُنطق. حارساه `tools/test_marks.mjs` و`browser_test.py --marks`.
- **جردُ أصناف كلمة الأساس** (`make_stories.py` — تصحيح المالك، ١٢ أغسطس ٢٠٢٦): صار في المولّد **«الزَّيْد»**، والعلَمُ لا تدخله «ال». **وهي ثالثةُ الصنف** بعد منع التنوين على لفظ الجلالة وبعد التطويل — وثلاثةٌ من صنفٍ واحد تعني أنّ الحالة تُعالَج والبابُ مفتوح. فجُرِد الصنفُ كلُّه مرّةً واحدة و**قُفِل الدورُ لا الحالة**: **عَلَم** (زيد · سامي · حسن · جحا) صورُه ثلاثٌ لا رابعَ لها — مجرَّدٌ و`~N` و`~A`؛ و**لفظُ الجلالة** معرفةٌ بذاته فلا «ال» ولا تنوين و`~u` وحدَه؛ و**المضافُ إلى ياء المتكلم** مجرَّدٌ لا غير («الْأَبِيُ» و«أَبِيٌ» ليستا عربيتين)؛ و**النكرة** هي الأصل تقبل الأدوار كلَّها. والمنعُ **يوقف البناء** ولا يمرّ، ومُجرَّبٌ سالباً على كل صنف. **والبابُ يُقفَل بالجرد لا بالقائمة**: يُثبِت الفحصُ أنّ كلَّ أصلٍ آخرُه ياءُ المتكلم مصنَّفٌ — مضافاً أو مستثنىً بعلّته (فعلٌ · نسبةٌ · منقوص) — فأصلٌ جديد بياءٍ بلا صنف يُسقِط الفحصَ **يومَ يُعلَن**.
- **مفاصلُ المرحلة القرآنية** (بلاغ المالك، ١٢ أغسطس ٢٠٢٦): كانت **٣١ عقدةً تحت عنوانٍ واحد** والبساتينُ مئةٌ في عشر محطاتٍ مسمّاة فلا تثقل — **فالعلّة كتلةٌ بلا مفاصل لا عددٌ كثير**. فشُقّت محطاتٍ مسمّاةً بعدّاداتها (`quranSections` في `progress.js`): **التهيئة** (الحرفان ودرجاتُ الكلمات) · **رسمُ المصحف** (الرسم والمقطَّعات) · **سورٌ قصار ١/٢/٣** (`SURAHS_PER_STATION` سورٍ لكلٍّ)، وقصةُ الفيل في موضعها تتبع سورتها. **والمحطاتُ محسوبةٌ من `QURAN.surahs` لا مكتوبة** — سورةٌ تُضاف تفتح محطتَها بلا سطرٍ يُعدَّل. **والشقُّ بلا أثر**: قسمةُ `quranNodes()` نفسِها بترتيبها، فالتسلسلُ المسطَّح هو هو حرفاً بحرف — لا ترتيبَ تغيّر ولا قفلٌ ولا نجمة، والترحيلُ بلا أثر. حارساه `test_quran.mjs` (بمقايسة المسطَّحَين) و`browser_test.py --map`.
- `tools/test_selftests.mjs` — **حارسُ الحرّاس «فحصٌ لا يُشغَّل ليس حارساً»** (أمر المالك، ١٢ أغسطس ٢٠٢٦، بعد أن تكرّر العيبُ ثلاثاً): يجرد `tools/` عن كل ما يُعلن `--self-test` **ويشغّله** — فما وُجد بالجرد دخل السَّوقة، وأداةٌ جديدة بفحصٍ رثّ تُسقِطه **يومَ تُكتب** لا بعد ثلاث حزم. وهو نظيرُ `test_measure` في بابه: ذاك يحرس المحطةَ بلا قياس، وهذا الفحصَ بلا مُشغِّل — وكلاهما يحرس **الغياب**. ومُجرَّبٌ ساعةَ كُتب: أمسك ثلاثةً خارج السَّوقة، وفيها `check_decodable.py` **أحمرُ بحقّ** (`== 4` سورةً مكتوبةً بيد، انكسرت يوم صارت اثنتي عشرة).
- `tools/test_measure.mjs` — **حارس «لا تدريسَ بلا قياس»** (METHOD §٦.٢): فجوةُ العلامات عاشت أربع عشرة حزمةً صامتة لأنّ **غياب القياس لا يُفشِل اختباراً**. فهذا الحارس يجرد الرحلة **نوعَ محطةٍ نوعَ محطة** ويطالب كلَّ محطةٍ تدرّس مهارةً بقياسٍ في ليتنر وتمرينٍ يراجعه وموضعٍ يقرؤه الوالد — أو بإعفاءٍ **مكتوبٍ بسببه** (القصةُ والمصحفُ يُقرآن ولا يُمتحَنان، والبوابةُ تقيس ولا تدرّس). جدولُ `STATIONS` فيه **هو العقد**: محطةٌ جديدة بلا قياسٍ ولا إعفاء تُسقِطه يومَ تُضاف.
- `app/js/gate.js` + محرّك الجلسة في `app/js/review.js` — **بوابتا الإتقان** (الحزمة ١٤): عقدتان قبل المرحلة القرآنية وقبل البساتين، جلسةُ مراجعةٍ إجبارية من **أضعف مهارات ليتنر** (`weakestSkills()` لا `dueSkills()`) بعشرة تمارين، والعبور بإصابة ≥٨٠٪ من المحاولات. **لا رسوب**: دونها تبقى التالية مقفلة مع إعادةٍ فورية بلا حدّ، وتُبنى تمارينها من جديد كل محاولة. بياناتها `GATES` في `curriculum.js`، وترحيلُها الرحيم في `migrateJourney()` بـ`progress.js` (مَن عبر المفصل قبل وجودها لا يُحبَس)، ويحرسها `tools/test_gate.mjs` و`browser_test.py --gate`. **صفر إضافة صوتية**: تمارينها تمارين المراجعة نفسها.
- `app/js/placement.js` — **«بوابة اللحاق»** (ملفُّ اللحاق، ١٦ أغسطس ٢٠٢٦): تلميذُ مدرسةٍ أو مركزٍ يصل بمستوىً قائم **يمتحن فيُفتح له ما أثبته ويقف حيث ينكسر**، بدل أن يمشي رحلةَ المبتدئ من الألف. **بابُه الوحيد قسمٌ في لوحة وليّ الأمر** (فلا يراه طفلٌ ولا يقع بنقرةٍ عابرة، ولا مسارَ له في `main.js`)، والامتحانُ جلسةُ ملء شاشةٍ بنسق البوابة. و**السلّمُ مشتقٌّ لا مكتوب، ودرجتُه شريحة** (حكمُ المدير أ على تسليم ل١): مجموعةٌ وما يتلوها حتى المجموعة التالية — درسُ علامةٍ ومحطةُ مواجهةٍ وقصة — ومادّتُها مفاتيحُ مهارات **عقدها كلِّها**، ثمانيةُ تمارين **بمُنشئات المراجعة القائمة** (صفرُ شكلِ تمرينٍ جديد و**صفرُ إضافةٍ صوتية** بالبناء)، والعتبةُ `passed` **المستوردةُ من `gate.js`** لا رقمٌ ثانٍ يفترق غداً. و**«لا تُفتح محطةٌ لم تُمَسّ»**: لكل محطةٍ في الشريحة سوى مجموعتها مفتاحان مضمونان **في كل محاولة** (`stationKeys` — والخلطةُ وحدَها تُضيّعهما في حوضٍ فيه عشراتُ مفاتيح الحروف)، فتُفتح الشريحةُ كاملةً وقد امتُحن في كلٍّ منها ويقف الطفلُ **عند شرخه بالضبط**؛ والقصةُ فيها تتبع بإعفاءٍ مكتوبٍ بسببه (تُقرأ ولا تُقاس). و**حدّان**: البواباتُ لا تُقفز (تُجتاز بنفسها)، والمرحلةُ القرآنية خارج السلّم كلياً — و`rungsOf` **دالّةٌ خالصة** يمتحن الحارسُ حدَّيها بأقسامٍ **مصنوعة**، إذ لا بوّابةَ اليومَ بين مجموعتين فالحدُّ الصامت لا يُعرَف صحيحاً من فاسد. وكلُّ محاولةٍ تدخل ليتنر **قياساً حقيقياً بلا وسمٍ خاصّ** (ولا تُقيَّد مراجعةَ يوم: امتحانُ موضعٍ لا جلسةُ تثبيت)، و**فتحٌ لا قفل**: النجمةُ الواحدة تفكّ القفل ولا تدّعي إتقاناً، والإعادةُ من اللوحة تستأنف من آخر حدّ. حرّاسُه `tools/test_placement.mjs` و`browser_test.py --placement`، وشاشتُه في عدّة الجهاز.
- `app/js/roots.js` + `ROOTS` في `curriculum.js` — **شبكات الجذور** (حزمة الجذور، METHOD §٥.٨): ثلاث عشرة عائلةً صرفية، لكلٍّ **سطرُ معنىً جامعٍ** منقولٌ من `docs/REVIEW_B2.md` حرفاً. و**العائلة عائلةُ معنى لا حروف**: أعضاؤها **مُعلَنون** لا يُشتقّون بمطابقة الرسم (يرفض `check_decodable.py` عضواً لا يطابق جذرُه المعلَن في مصدره)، و«روح» شُقَّت فخرجت منها «رَاحَة»، و«ركب» فخرجت «رُكْبَة» — وهما `stranger` **مشتّتُ «الحروف بلا المعنى»**، أذكى ما في «اجمع العائلة». **وموضعُ كل شجرةٍ محسوبٌ لا مكتوب** (`rootPlacement` في `progress.js`): حيث يكتمل ثالثُ عضوٍ مدروس، فإضافةُ كلمةٍ تحرّك الموضعَ وحدَه. و**لا يُعرَض غير المدروس أصلاً** (لا قفلٌ مرئيّ)، و**حوضُ الخيارات ز١ دائماً** بنيوياً (لا يمرّ بـ`fadeWord` ولا `textWord`). القياسُ نوع `root` بمفتاح `root-<العائلة>` — يدخل ليتنر والمراجعة (`rootItem`) والبوابتين، وله **قسمُه** في لوحة وليّ الأمر (فلا حرفَ وهميّ فيها). حارساه `tools/test_roots.mjs` و`browser_test.py --roots`.
- `app/js/contrast.js` — **محطتا «ميّز بين»** (الحزمة ١٣): مواجهةُ المتشابهات بعد أن باعد المنهجُ بينها (METHOD §٢.٥). بياناتُها `CONTRASTS` في `curriculum.js` — سبعةُ أزواج في محطتين: بعد المجموعة ٦ (س/ص · ت/ط · ك/ق) وبعد ٧ (ذ/ظ/ز · د/ض · ث/س · هـ/ح)، **ولكلٍّ موضعُ اكتمالِ حروفها** (يرفض `check_decodable.py` زوجاً يسبق حرفَه). خطوتان: «قارِن» ثم جولتان لكل زوج، الخياران هما الزوج نفسُه بحركةٍ متطابقة. **صفرُ إضافةٍ صوتية** (حروفٌ بحركاتها لها ملفاتها منذ الجلسة ١)، والقياس بنوع `contrast` يدخل ليتنر **وله تمرينُه في المراجعة والبوابة** (`contrastItem` في `review.js`) فلا تكذب لوحةُ وليّ الأمر. و**«اسمع الفرق»** استثناءٌ معلَن من قاعدة «لا تلقين للصواب» (`compareSounds`، DESIGN §٥.٥). يحرسها `tools/test_contrast.mjs` و`browser_test.py --contrast`.
- `app/js/recorder.js` + `app/js/recordings.js` + `app/js/record.js` — «اقرأ لي» (الحزمة ١٠): التقاط صوت الطفل وهو يقرأ (MediaRecorder) وخزنُه في IndexedDB بحصةٍ تُقلَّم. **ثلاث وحدات لا تعرف الشبكة ولا `audio.js`** — انظر قاعدة الخصوصية أدناه، ويحرسها `tools/test_recordings.mjs` و`tools/browser_record.html`. ومدد القراءات (مؤشر الطلاقة) في `progress.js` لا في الصوت، فتبقى بعد تقليمه. و**`record.js` هو كتلةُ الزرّ الواحدة** لموضعيها — القصة وترديدُ السورة (حزمة «القرآني الموسّع») — فلا تُنسخ شيفرةٌ تحمل صوتَ طفل: يومَ يُشدَّد قيدٌ في نسخةٍ تبقى الأخرى على حالها، والخصوصيةُ لا تحتمل نسختين تفترقان. والشاشتان تستدعيانها ولا تحملانها (يحرسه `test_quran.mjs` بغياب `blob` من `quran.js` و`story.js`).
- **«القرآني الموسّع»** (١١ أغسطس ٢٠٢٦، حكم المدير على `docs/REVIEW_QURAN_SLICE.md`): المرحلة صارت **اثنتي عشرة سورة** — ثمانٍ جديدة بمعيارَي الطول التصاعدي (٩٥ ← ٣٢٨ رمزاً) والرصيد الحرفي، لكلٍّ عقدُها الكامل والقفلُ التسلسليّ ممتدّاً إليها. ودرسُ الرسم **٩ علامات** (بـ`ۦ` أختِ `ۥ` و`ۭ` أختِ `ۢ` و`۟` الدائرة) ودرسُ الهمزة **٥ صور** (بـ`ؤ` و`ئ` وكلمتيهما) — **وترحيلُهما رحيم**: مَن تجاوزهما لا يُقفَل عليه شيء والامتدادُ يُرى بالعودة إليهما. و**خطوةُ «الترديد»** ثالثةً في شاشة السورة لا عقدةً: يسمع الآية بتلاوة الحصري ثم يردّدها، **بلا مؤقّتٍ ألبتّة** والانتقالُ بيده، ومعها «رتّل وسجّل» بكتلة `record.js` نفسِها — وهي **معفاةٌ من القياس بنصٍّ مكتوبٍ بسببه** في `test_measure.mjs` (المصحفُ يُتلى ولا يُمتحَن). **والتين والقدر مستثناتان بحكمٍ مستقلّ**: بسملتُهما في المصدر `بِّسْمِ` بشدّةٍ على الباء (إظهارُ إدغام باء آخرِ السورة قبلهما) فلا تُنزَع بمطابقةٍ حرفية. حارساه `tools/test_quran.mjs` و`browser_test.py --quran`.
- `tools/icon_audit.py` — مهمة «صدق الصورة»: جرد كل أزواج (كلمة، صورة) في المصادر الثلاثة (٤٠٥ زوجاً) بثلاث درجات صرامة **بحسب موضع الصورة من الشاشة** (إجابة/بطاقة/زينة)، وحارسٌ آليّ: **لا صورتان متطابقتان لكلمتين في حوض سؤالٍ واحد** (خطأ)، مع تنبيه الالتباس المتبادَل بأُسَرٍ بصرية معلَنة وتنبيه التعارض عبر الرحلة. المبدأ في `docs/DESIGN.md` §٦، وبوّابةُ العين في `docs/REVIEW_ICONS.md` — **ولا تُغيَّر صورةٌ قبل إقرار المالك والمدير**. وأحكامُ المدير مطبَّقة (٦ أغسطس ٢٠٢٦): ١٤ بديلاً، و**حقل `pictured: false`** — إعلانُ الكلمة التي لا تصوّرها صورةٌ صادقة، تستهلكه أحواض الأسئلة الأربعة (`screens.js` و`ladder.js` و`sentences.js` و`library.js`) فلا تُعرَض إجابةً ولا خياراً مصوَّراً وتبقى في بطاقتها والتهجئة والجمل؛ يحرسه `tools/test_icons.mjs`.
- `tools/browser_device.html` + `--device` في `browser_test.py` — **عدّة الجهاز** (مهمة «صقل الجهاز»): شاشةُ التطبيق نفسُها بمقاس آيباد حقيقيّ (خمسة مقاسات، طولاً وعرضاً) تقيس الفائض الرأسي والأفقي شاشةً شاشةً، و`--metrics` تعاير توسيط الحرف البطل، و`--shots --screen` تلتقط لقطة جهازٍ واحدة (و`--flat` تعطّل رفعة الحرف لصورة «قبل»). المبدآن في `docs/DESIGN.md` §٤: الطولي مرجعٌ لا يُمسّ، والعرضيّ شقّان بلا سحب (`heroStep`)؛ ومركزُ حبر الحرف في مركز صندوقه (`giantInk`).
- `ref/` — الكتابان المرجعيان: القاعدة النورانية والدروس الهجائية (PDF)

## قواعد ثابتة
- **الصوت مولّد مسبقاً ومخزون**: كل الأصوات ملفات في `app/audio/` تُنتَج مرة واحدة بسكربت `tools/` وتُحفظ لإعادة الاستعمال — ممنوع أي توليد صوت لحظي من التطبيق أو استدعاء أي API صوتي وقت التشغيل (احتياط Web Speech عند غياب ملف فقط).
- **كل طلب صوت موسوم ببصمة محتواه**: اسم الملف من **نصّه** لا من محتواه، فالاستبدال تحت المفتاح نفسه لا يغيّر الرابط — ويبقى مَن خزّن القديم عليه أبداً. لذلك يكتب المولّد `app/audio/versions.json` (ويكتب `fetch_recitation.py` نظيرَه `v` في `app/data/recitations.json`)، ويطلب `audio.js` و`recitation.js` بها `<key>.mp3?v=<بصمة>`، ويخزن `sw.js` بالرابط الموسوم كانساً الوسمَ الأقدم. يحرسه `tools/test_audio_cache.mjs`.
- **مخزنُ الصوت يعبر النسخ، والقشرةُ وحدها موسومة** (حزمة «خفّة التخزين»، ١١ أغسطس ٢٠٢٦): اسمُه `muallim-audio` **بلا `VERSION`** — وكان موسوماً بها فيولد كلَّ حزمةٍ مخزنٌ فارغ ويُحذف السابق، فيعيد جهازُ الطفلة تنزيل الصوت كلِّه (٤١ ميغابايت) في كل تحديث؛ وهو هدرٌ محض لأن بصمةَ المحتوى في الرابط تحكم الطزاجة سلفاً. ومعه ثلاثة: **لا يُجلب إلا الناقص** (`cache.add` يجلب دائماً وإن كان مخزوناً)، و**دفعاتٌ من ١٦** لا قطيعُ ٢٧٨٧ طلباً مرتَّبةً بطول النصّ (أثرُ صعود المنهج) والتلاوةُ آخِراً، و**الإخفاقُ يُعدّ ولا يُبتلَع** فيوقف الكنسَ (صيانةً للقديم الصالح) ويظهر عدداً في لوحة وليّ الأمر بـ`progress.audioStored()` — **مقيساً من المخزن نفسِه بـ`caches.match` لا بـ`fetch`** (عهدُ «صفر طلبٍ شبكيّ في دورة التسجيل» مطلق). حرّاسُه ثلاث طبقات: `test_pwa.mjs` (الشكل) و`test_audio_cache.mjs` (السلوك على `sw.js` الحيّ بترقيةٍ حقيقية) و`browser_test.py --parent` (Chrome، بعدّاد طلبات mp3 **عند الخادم**).
- **فصل الصوت عن التطوير** (`docs/AUDIO_QUEUE.md` — ملزم): جلسات التطوير **لا تشغّل مولّد الأصوات أبداً ولا تلمس `app/audio/`**؛ النصوص المنطوقة الجديدة تُضاف إلى `tools/audio_queue.json` وتصرّفها جلسة الصوتيات وحدها على حصة كل يوم. فحص «لا نطق آلي» يستثني نصوص القائمة `pending` مؤقتاً.
- **تسجيلات الطفل لا تغادر جهازه أبداً** (خصوصية مطلقة، الحزمة ١٠): صوتُ الطفل يُلتقط في `app/js/recorder.js` ويُخزَن في IndexedDB محلياً (`app/js/recordings.js`) ويُسمع من عنوان `blob:` — **لا رفع ولا شبكة ولا تحليل خارجي ولا خزنٌ في عامل الخدمة**. الوحدات الحاملة (`recorder.js` و`recordings.js` و`record.js` وحدةُ الزرّ المشتركة) لا تعرف الشبكة أصلاً (لا `fetch` ولا `XMLHttpRequest` ولا `sendBeacon`)، ويحرس ذلك حارسان: `tools/test_recordings.mjs` يقرأ نصّ كل ملفٍّ يمرّ به الصوت، و`browser_test.py --record` يثبت **صفر طلبات شبكية** في دورة التسجيل كلها.
- لا كلمة/جملة تعرض حرفاً أو علامة لم تُدرَّس بعد (مفكوكية ١٠٠٪).
- **نصّ المصحف**: لا يُكتب بيدنا ولا يُنسخ من شاشة — مصدره `tools/quran_source.txt` وينقله `tools/sync_quran.py`، ويرفض الفاحص أي فرق. و**لا يُنطق آلياً أبداً** (METHOD §٥.٦: التلاوة بصوت قارئ متقن) — انظر `docs/AUDIO_QUEUE.md`.
- واجهة RTL عربية بالكامل، أزرار كبيرة تناسب طفلاً، تعمل باللمس والفأرة.
- HTML/CSS/JS خالص بوحدات ES — لا إطارات عمل ولا npm.
- التقدّم في localStorage عبر `app/js/progress.js`.

## بريد العائلة — لجلسة الإدارة وحدها (تصحيح المالك، ١١ أغسطس ٢٠٢٦)
- البريدُ شأنُ **جلسة الإدارة (فابل) وحدَها** — جلساتُ التنفيذ لا تقرؤه ولا تكتب فيه: حدودُ بند جلستها تكفيها، وما يعنيها من بلاغٍ يُدخله المديرُ في بند جلستها.
- إن كنتَ جلسةَ الإدارة: في **أول كل جلسة** اقرأ ما استجدّ في `../family/bulletins/` (عقدُ البريد في `../family/README.md`)، وقيّد إيصالَ كل بلاغٍ في `docs/FAMILY_MAIL.md` (اسم الملف · قُرئ بتاريخ · ما فُعل به). **بلاغُ إجراءٍ لا يُغلق إلا بإيصاله.**
- وبلاغُك للعائلة يُكتب ملفاً هناك ويُلتزم — لا يُنقل عبر المالك.
