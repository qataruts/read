# مشروع «المُعلِّم» — تعليم القراءة العربية لطفل السادسة

تطبيق ويب (دون أدوات بناء) يعلّم الطفل القراءة على أساس القاعدة النورانية مطوَّعةً:
ترتيب الحروف بالتواتر، كلمات من عالم الطفل، مفكوكية ١٠٠٪، بمسار واحد ينتهي بالتهيئة القرآنية.

## طريقة العمل — اقرأ هذا أولاً
- **مدير المشروع**: جلسة فابل (Fable) — تضع المنهج والخطة وتراجع المنتجات. **جلسات التنفيذ**: أوبوس ٥.
- إن كنت جلسة تنفيذ: اقرأ `docs/METHOD.md` (المرجع الملزم للمنهج) ثم بند جلستك في `docs/SESSIONS.md`، ونفّذ حدود جلستك فقط، وحدّث حالة الجلسة عند الإتمام.

## خريطة الملفات
- `docs/METHOD.md` — المنهج التعليمي الملزم (المجموعات، حلقة الدرس، التدرج، استراتيجية الصوت)
- `docs/SESSIONS.md` — خطة الجلسات ومعايير قبولها وسجل حالتها
- `docs/AUDIO_QUEUE.md` — بروتوكول فصل الصوت عن التطوير (ملزم)
- `app/js/curriculum.js` — **مصدر الحقيقة الوحيد** لبيانات المنهج (حروف/مجموعات/كلمات/مقاطع/مهارات/قصص/المرحلة القرآنية)
- `app/data/lexicon.json` — معجم «حديقة الكلمات» (طبقة ب١): ٢٥٠ كلمة في عشرة بساتين، ومعها **جمل سلّم الجمل** (جملةُ مثالٍ لكل كلمة + `sentences`: ١٦٠ جملة متدرّجة من ٣–٥ كلمات) و`support` (معجم الجمل المساند المعلَن). يقرؤه `app/js/lexicon.js` ويفحصه `tools/check_lexicon.py`. **يوسّع رصيد المنهج ولا يكرّره** (يرفض الفاحص كلمةً درسها الطفل في `curriculum.js`)، و**مقاطعه مشتقّة** بمقطِّع نورانية لا مكتوبة بيد (`--fill-tiles`).
- `tools/make_sentences.py` — مولّد «الجمل المتدرجة» المقيَّد (الحزمة ٩أ، وهي بروفة عقد مصنع القصص): **لا يُكتب فيه حرفٌ مشكولٌ بيد** — الجملة قائمةُ كلماتِ أساسٍ معلَنة + أدوارٍ إعرابية، والصيغة تُشتقّ. خطّ الإنتاج: توليد ← `check_lexicon.py` يرفض ما خالف ← مراجعة المدير (`tools/review_sentences.mjs` ← `docs/REVIEW_9A.md`). و`--check` يثبت أن ما في المعجم هو عينُ خرج المولّد.
- `app/sw.js` + `app/manifest.webmanifest` + `app/icons/` — العمل دون إنترنت (PWA)؛ الأيقونات تُولَّد بـ`tools/make_icons.py` من `tools/icon.html`
- `tools/generate_audio.py` — توليد أصوات mp3 إلى `app/audio/` (استبدالها بتسجيلات بشرية لاحقاً لا يغيّر الشيفرة)
- `tools/quran_source.txt` + `tools/sync_quran.py` — نصّ المصحف المرجعي (مشروع تنزيل، الرسم العثماني) وأداة نسخه إلى المنهج حرفاً بحرف
- `tools/fetch_recitation.py` → `app/data/recitations.json` + `app/js/recitation.js` — تلاوة القارئ (الحصري المرتّل) لآيات المرحلة القرآنية: **مسار مستقلّ عن المولّد وعن `manifest.json`**، وحدَه يُسمع نصَّ المصحف (لا مولّد ولا نطق آلي — انظر `AUDIO_QUEUE.md`)
- `ref/` — الكتابان المرجعيان: القاعدة النورانية والدروس الهجائية (PDF)

## قواعد ثابتة
- **الصوت مولّد مسبقاً ومخزون**: كل الأصوات ملفات في `app/audio/` تُنتَج مرة واحدة بسكربت `tools/` وتُحفظ لإعادة الاستعمال — ممنوع أي توليد صوت لحظي من التطبيق أو استدعاء أي API صوتي وقت التشغيل (احتياط Web Speech عند غياب ملف فقط).
- **فصل الصوت عن التطوير** (`docs/AUDIO_QUEUE.md` — ملزم): جلسات التطوير **لا تشغّل مولّد الأصوات أبداً ولا تلمس `app/audio/`**؛ النصوص المنطوقة الجديدة تُضاف إلى `tools/audio_queue.json` وتصرّفها جلسة الصوتيات وحدها على حصة كل يوم. فحص «لا نطق آلي» يستثني نصوص القائمة `pending` مؤقتاً.
- لا كلمة/جملة تعرض حرفاً أو علامة لم تُدرَّس بعد (مفكوكية ١٠٠٪).
- **نصّ المصحف**: لا يُكتب بيدنا ولا يُنسخ من شاشة — مصدره `tools/quran_source.txt` وينقله `tools/sync_quran.py`، ويرفض الفاحص أي فرق. و**لا يُنطق آلياً أبداً** (METHOD §٥.٦: التلاوة بصوت قارئ متقن) — انظر `docs/AUDIO_QUEUE.md`.
- واجهة RTL عربية بالكامل، أزرار كبيرة تناسب طفلاً، تعمل باللمس والفأرة.
- HTML/CSS/JS خالص بوحدات ES — لا إطارات عمل ولا npm.
- التقدّم في localStorage عبر `app/js/progress.js`.
